$define model_path "models/box.obj"

m = model_path
t = "textures/block.png"

cube {
    mesh(m, TEXTURED, t, [0, 0, 0], [255, 255, 255])
    pos(0, 0, 0.1)
}

cube2 {
    mesh(m, TEXTURED, t, [0, 0, 0], [255, 0, 0])
    pos(0, 0, -0.1)
}

place(cube)
place(cube2)